# Enforce LF line endings for all text files.
# This prevents Windows Git from converting LF -> CRLF on checkout,
# which would break AI-assisted edits (TargetContent string matching expects LF).
* text=auto eol=lf
